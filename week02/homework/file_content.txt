import socket

HOST = 'localhost'
PORT = 10000


def echo_client():
    """Echo Server 的 Client端 """

    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.connect((HOST, PORT))

    while True:
        # 接收用户输入数据并发送服务器